{{writeHeader}}

import { it, expect, describe } from "vitest";

describe("Modules / {{pascalCase moduleName}} / API / Query", () => {
	it("TODO: Add test cases", () => {
		expect(1 + 1).toBe(2)
	});
});
