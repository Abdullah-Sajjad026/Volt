{{writeHeader}}

import { it, expect, describe } from "vitest";

describe("Modules / {{pascalCase moduleName}} / Context", () => {
	it("TODO: Add test cases", () => {
		expect(1 + 1).toBe(2)
	});
});
